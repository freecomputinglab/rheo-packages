// @rheo/rookery-search — the search bar's browser half.
//
// Bundled by vite into `dist/lib.js` as an IIFE exposing the global
// `RheoRookerySearch`, and injected by rheo from this package's `js_scripts`
// manifest key.

export const VERSION = "0.1.0";
