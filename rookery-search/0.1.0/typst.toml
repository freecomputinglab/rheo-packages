[package]
name = "rookery-search"
version = "0.1.0"
compiler = "0.15.0"
entrypoint = "dist/lib.typ"
authors = ["The Free Computing Lab <https://freecomputinglab.ohrg.org>"]
license = "MIT"
description = "Fuzzy search over @rheo/rookery notes — Typst primitive, JSON index, and an embeddable search bar"
repository = "https://github.com/freecomputinglab/rheo-packages"

# THE RHEO FLOOR, not a rookery version. It was `0.5.2` and that was wrong rather
# than merely stale: this package hard-imports `@rheo/rookery` (`src/lib.typ`,
# `src/lookup.typ`), whose own manifest declares `min_version = "0.6.0"` — so a
# project on rheo 0.5.2 satisfied what this said, then failed on the rookery it pulled
# in. The other three packages in the family already said 0.6.0; this was the odd one
# out. Its own readme states the dependency in prose too.
[tool.rheo]
min_version = "0.6.0"

[tool.rheo.html]
js_scripts = "dist/lib.js"
css_stylesheet = "dist/rookery-search.css"
