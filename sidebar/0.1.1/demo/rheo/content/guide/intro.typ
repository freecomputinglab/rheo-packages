#import "../_template.typ": template
#show: template

= Intro

A vertebra one directory deep, so its handle is `guide:intro` and rheo writes
it to `guide/intro.html` — a DIRECTORY, not a `guide:intro.html` filename.
