#import "_template.typ": template
#show: template

= Index

The site root. Its nav links are written without a `../` prefix; the same nav
on a page inside `guide/` carries one, which is what `demo/check.sh` asserts.
